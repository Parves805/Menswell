diff --git a/node_modules/react-to-print/lib/index.js b/node_modules/react-to-print/lib/index.js
index 455e714..b75f144 100644
--- a/node_modules/react-to-print/lib/index.js
+++ b/node_modules/react-to-print/lib/index.js
@@ -69,13 +69,21 @@
         this.handlePrint = () => {
             const { onBeforeGetContent, onBeforePrint, } = this.props;
             if (onBeforeGetContent) {
-                const onBeforeGetContentReturned = onBeforeGetContent();
-                if (onBeforeGetContentReturned && typeof onBeforeGetContentReturned.then === 'function') {
-                    onBeforeGetContentReturned.then(() => {
-                        this.handlePrintContent(onBeforePrint);
-                    });
-                }
-                else {
-                    this.handlePrintContent(onBeforePrint);
-                }
+                // Prevent calling findDOMNode for multiple re-renders.
+                // https://github.com/gregnb/react-to-print/pull/422
+                // https://github.com/gregnb/react-to-print/issues/424
+                if (this.gettingContent) {
+                    return;
+                }
+                this.gettingContent = true;
+                const onBeforeGetContentReturned = onBeforeGetContent();
+                Promise.resolve(onBeforeGetContentReturned)
+                    .then(() => {
+                    // Timeout helps ensure that content has updated
+                    setTimeout(() => {
+                        this.handlePrintContent(onBeforePrint);
+                        this.gettingContent = false;
+                    }, 500);
+                });
             }
             else {
                 this.handlePrintContent(onBeforePrint);
diff --git a/node_modules/react-to-print/lib/index.mjs b/node_modules/react-to-print/lib/index.mjs
index 683e357..a857008 100644
--- a/node_modules/react-to-print/lib/index.mjs
+++ b/node_modules/react-to-print/lib/index.mjs
@@ -62,13 +62,21 @@
         this.handlePrint = () => {
             const { onBeforeGetContent, onBeforePrint, } = this.props;
             if (onBeforeGetContent) {
-                const onBeforeGetContentReturned = onBeforeGetContent();
-                if (onBeforeGetContentReturned && typeof onBeforeGetContentReturned.then === 'function') {
-                    onBeforeGetContentReturned.then(() => {
-                        this.handlePrintContent(onBeforePrint);
-                    });
-                }
-                else {
-                    this.handlePrintContent(onBeforePrint);
-                }
+                // Prevent calling findDOMNode for multiple re-renders.
+                // https://github.com/gregnb/react-to-print/pull/422
+                // https://github.com/gregnb/react-to-print/issues/424
+                if (this.gettingContent) {
+                    return;
+                }
+                this.gettingContent = true;
+                const onBeforeGetContentReturned = onBeforeGetContent();
+                Promise.resolve(onBeforeGetContentReturned)
+                    .then(() => {
+                    // Timeout helps ensure that content has updated
+                    setTimeout(() => {
+                        this.handlePrintContent(onBeforePrint);
+                        this.gettingContent = false;
+                    }, 500);
+                });
             }
             else {
                 this.handlePrintContent(onBeforePrint);
